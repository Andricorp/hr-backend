const procedures = 0;
const functions = 0;
const createPool = require('./core/connection');
const init = require('./core/init');

module.exports = {
    procedures,
    functions,
    init,
    createPool
};
